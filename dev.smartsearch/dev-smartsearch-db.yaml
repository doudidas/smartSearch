apiVersion: v1
kind: Namespace
metadata:
  name: smartsearch
---
apiVersion: v1
kind: Service
metadata:
  namespace: smartsearch
  name: dev-smartsearch-db
  labels:
    app: smartSearch
    stage: development
    tier: database
spec:
  ports:
    - name: mongo
      port: 27017
      protocol: TCP
      targetPort: 27017
  selector:
    app: smartSearch
    stage: development
    tier: database
  type: ClusterIP
---
apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: dev-smartsearch-db
  namespace: smartsearch
  labels:
    app: smartSearch
    stage: development
    tier: database
spec:
  serviceName: dev-smartsearch-db
  replicas: 1
  template:
    metadata:
      labels:
        app: smartSearch
        stage: development
        tier: database
    spec:
      terminationGracePeriodSeconds: 10
      containers:
        - name: mongo
          image: mongo:3
          command:
            - mongod
            - "--replSet"
            - rs0
            - "--bind_ip"
            - 0.0.0.0
            - "--smallfiles"
            - "--noprealloc"
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-persistent-storage-dev
              mountPath: /data/db
        - name: mongo-sidecar
          image: cvallance/mongo-k8s-sidecar
          env:
            - name: MONGO_SIDECAR_POD_LABELS
              value: "app=smartSearch,stage=development,tier=database"
  volumeClaimTemplates:
  - metadata:
      name: mongo-persistent-storage-dev
    spec:
      accessModes: [ "ReadWriteOnce" ]
      volumeMode: Filesystem
      resources:
        requests:
          storage: 1Gi