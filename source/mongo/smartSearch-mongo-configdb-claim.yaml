apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-configdb-claim
  labels:
    type: Database
    app: mongo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
  storageClassName: manual
status: {}

