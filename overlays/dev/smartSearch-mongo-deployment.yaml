apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
  labels:
    stage: dev
spec:
  selector:
    matchLabels:
      stage: dev
  template:
    metadata:
      labels:
        stage: dev
    spec:
      containers:
      - image: mongo:latest
        name: mongo