apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
spec:
  selector:
    matchLabels:
      app: web
  replicas: 3
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: web
    spec:
      containers:
      - image: spacelama/web:latest
        name: spacelama-web
        ports:
        - containerPort: 4200
        resources: {}
      restartPolicy: Always
status: {}
