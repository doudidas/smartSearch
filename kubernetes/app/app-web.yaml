apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: web
spec:
  ports:
  - name: "443"
    port: 443
    targetPort: 4200
  selector:
    app: web
  type: LoadBalancer
status:
  loadBalancer: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
spec:
  selector:
    matchLabels:
      app: web
  replicas: 3
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: web
    spec:
      containers:
      - image: spacelama/web:latest
        name: spacelama-web
        ports:
        - containerPort: 4200
        resources: {}
      restartPolicy: Always
status: {}
